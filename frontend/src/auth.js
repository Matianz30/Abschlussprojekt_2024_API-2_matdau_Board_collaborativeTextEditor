//Export if logged in or not in react to route to login
//This if info from Chatgpt so the authentication works together with react
/*export const checkAuth = async () => {
  const response = await fetch("http://localhost:3001/status", {
    credentials: "include",
  });
  const data = await response.json();
  return data.loggedIn;
};*/
